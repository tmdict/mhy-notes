Data is stored locally in this browser and will be gone permanently if browser's cache is cleared. Export a JSON backup
of the data and use it to sync your progress across different browsers and devices. Imported data will overwrite all
existing data.
